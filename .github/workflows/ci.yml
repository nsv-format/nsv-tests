name: CI

on:
  push:
  pull_request:
    branches: [master]

jobs:
  check-fixtures:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: scripts/check_fixtures.sh
